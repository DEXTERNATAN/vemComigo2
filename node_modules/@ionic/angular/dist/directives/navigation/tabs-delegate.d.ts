import { ElementRef } from '@angular/core';
import { Router } from '@angular/router';
export declare class TabsDelegate {
    private router;
    constructor(router: Router, elementRef: ElementRef);
    onTabbarClick(ev: UIEvent): void;
}
